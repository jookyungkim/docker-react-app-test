sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t joo1004/docker-react-app-v2 -f Dockerfile.dev

script:
  - docker run -e CI=true joo1004/docker-react-app-v2 npm run test -- --coverage

after_success:
  - echo "Test Success"
